# BREWLUXE Architecture Diagrams

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT SIDE (Browser)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    coffee.html                           │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │ document.addEventListener('DOMContentLoaded', ...)│  │   │
│  │  │ ├─ initApp() [ASYNC]                              │  │   │
│  │  │ │  ├─ await fetchMenuItems()                      │  │   │
│  │  │ │  ├─ await fetchProducts()                       │  │   │
│  │  │ │  ├─ renderMenu()        ← Uses fetched data     │  │   │
│  │  │ │  ├─ renderProducts()    ← Uses fetched data     │  │   │
│  │  │ │  ├─ renderGallery()     ← Static data           │  │   │
│  │  │ │  ├─ renderReviews()     ← Static data           │  │   │
│  │  │ │  └─ setupEventListeners()                       │  │   │
│  │  │ │     ├─ Cart management                          │  │   │
│  │  │ │     ├─ Favorites (localStorage)                 │  │   │
│  │  │ │     ├─ Loyalty points (localStorage)            │  │   │
│  │  │ │     └─ Checkout form submission                 │  │   │
│  │  │ └────────────────────────────────────────────────┘  │   │
│  │  │                                                       │   │
│  │  │  API_BASE_URL = 'http://localhost:3000/api'        │   │
│  │  │                                                       │   │
│  │  │  let menuItems = []    ← Filled by fetchMenuItems()│   │
│  │  │  let products = []     ← Filled by fetchProducts() │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                           │   │
│  │  HTTP Fetch Calls:                                      │   │
│  │  ├─ GET  /api/menu       → Array of menu items         │   │
│  │  ├─ GET  /api/products   → Array of products           │   │
│  │  ├─ POST /api/orders     → Create order (checkout)     │   │
│  │  └─ POST /api/payment/... → Process payment (mocked)   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTP (with CORS)
                              ↓ (Port 3000)
┌─────────────────────────────────────────────────────────────────┐
│                      SERVER SIDE (Node.js)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Express.js Server (server.js)              │   │
│  │                                                           │   │
│  │  ┌─ CORS Middleware ──────────────────────────────────┐ │   │
│  │  │  Allows http://localhost:5520 to call API          │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  ┌─ Menu Routes ──────────────────────────────────────┐ │   │
│  │  │  GET    /api/menu         → All menu items        │ │   │
│  │  │  GET    /api/menu/:id     → Single item           │ │   │
│  │  │  POST   /api/menu         → Create item           │ │   │
│  │  │  PUT    /api/menu/:id     → Update item           │ │   │
│  │  │  DELETE /api/menu/:id     → Delete item           │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  ┌─ Products Routes ──────────────────────────────────┐ │   │
│  │  │  GET    /api/products     → All products          │ │   │
│  │  │  GET    /api/products/:id → Single product        │ │   │
│  │  │  POST   /api/products     → Create product        │ │   │
│  │  │  PUT    /api/products/:id → Update product        │ │   │
│  │  │  DELETE /api/products/:id → Delete product        │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  ┌─ Orders Routes ────────────────────────────────────┐ │   │
│  │  │  GET    /api/orders       → All orders            │ │   │
│  │  │  POST   /api/orders       → Create order          │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  ┌─ Admin Routes ─────────────────────────────────────┐ │   │
│  │  │  POST   /api/admin/seed   → Reset with sample    │ │   │
│  │  │  GET    /api/admin/stats  → DB statistics        │ │   │
│  │  │  GET    /api/health       → Server status        │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              ↓                                    │
│         SQLite Database Driver (sqlite3 npm package)             │
│                              ↓                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE SIDE (SQLite)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  File: brewluxe.db (auto-created on first server start)         │
│                                                                   │
│  ┌─ menu_items Table ─────────────────────────────────────┐    │
│  │  id (PRIMARY KEY)                                       │    │
│  │  name TEXT                                              │    │
│  │  description TEXT                                       │    │
│  │  price REAL                                             │    │
│  │  image TEXT                                             │    │
│  │  category TEXT (espresso, latte, cold, dessert)       │    │
│  │  createdAt DATETIME                                     │    │
│  │  updatedAt DATETIME                                     │    │
│  │                                                         │    │
│  │  [Espresso, Cappuccino, Caramel Latte, ...] (8 items) │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌─ products Table ───────────────────────────────────────┐    │
│  │  id (PRIMARY KEY)                                       │    │
│  │  name TEXT                                              │    │
│  │  description TEXT                                       │    │
│  │  price REAL                                             │    │
│  │  rating INTEGER (1-5 stars)                             │    │
│  │  image TEXT                                             │    │
│  │  createdAt DATETIME                                     │    │
│  │  updatedAt DATETIME                                     │    │
│  │                                                         │    │
│  │  [Ethiopian Blend, Colombian, Italian, ...] (6 items) │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌─ orders Table ──────────────────────────────────────────┐    │
│  │  id (PRIMARY KEY)                                        │    │
│  │  orderNumber TEXT (UNIQUE)                              │    │
│  │  customerName TEXT                                       │    │
│  │  customerEmail TEXT                                      │    │
│  │  customerPhone TEXT                                      │    │
│  │  orderType TEXT (pickup, delivery)                       │    │
│  │  items TEXT (JSON array of order items)                 │    │
│  │  total REAL                                              │    │
│  │  status TEXT (pending, completed, cancelled)            │    │
│  │  createdAt DATETIME                                      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram - Page Load

```
┌──────────────────────────────────────────────────────────────────────────┐
│ User opens: http://localhost:5520/coffee.html                             │
└──────────────────────────────────────────────────────────────────────────┘
                                    ↓
        ┌───────────────────────────────────────────────────┐
        │ Browser downloads coffee.html from port 5520      │
        │ - Parses HTML                                      │
        │ - Loads CSS                                        │
        │ - Loads JavaScript                                 │
        └───────────────────────────────────────────────────┘
                                    ↓
        ┌───────────────────────────────────────────────────┐
        │ DOMContentLoaded event fires                       │
        │ ├─ Calls: initApp()                              │
        │ └─ [ASYNC - returns Promise]                      │
        └───────────────────────────────────────────────────┘
                                    ↓
        ┌───────────────────────────────────────────────────┐
        │ initApp() execution:                              │
        │ 1. loadState() - restore from localStorage        │
        │ 2. renderGallery() - static images               │
        │ 3. Show loading spinners                          │
        └───────────────────────────────────────────────────┘
                                    ↓
        ┌─────────────────────────────────────────────────────────────┐
        │ await fetchMenuItems()                                       │
        │ ├─ fetch('http://localhost:3000/api/menu')                 │
        │ ├─ Wait for response...                                     │
        │ ├─ Convert to JSON                                          │
        │ └─ Store in: menuItems = [...]                             │
        └─────────────────────────────────────────────────────────────┘
                    ↓                              ↑
            HTTP GET request          Backend returns 8 items
            to port 3000              (with CORS headers)
                    ↓                              ↑
        ┌───────────────────────────────────────────────────┐
        │ Backend receives request at /api/menu             │
        │ ├─ Execute: SELECT * FROM menu_items             │
        │ ├─ Database returns 8 menu items                  │
        │ └─ Send JSON response with CORS headers           │
        └───────────────────────────────────────────────────┘
                                    ↓
        ┌─────────────────────────────────────────────────────────────┐
        │ await fetchProducts()                                        │
        │ ├─ fetch('http://localhost:3000/api/products')              │
        │ ├─ Wait for response...                                     │
        │ ├─ Convert to JSON                                          │
        │ └─ Store in: products = [...]                              │
        └─────────────────────────────────────────────────────────────┘
                    ↓                              ↑
            HTTP GET request          Backend returns 6 items
            to port 3000              (with CORS headers)
                                ↓
                ┌─────────────────────────────────────────────┐
                │ Both API calls complete with data           │
                └─────────────────────────────────────────────┘
                                    ↓
        ┌───────────────────────────────────────────────────┐
        │ Continue initApp() execution:                     │
        │ 4. renderMenu(menuItems)     - build HTML        │
        │ 5. renderProducts(products)  - build HTML        │
        │ 6. renderReviews()           - static data       │
        │ 7. Update cart, favorites, loyalty display       │
        │ 8. Start review carousel                         │
        │ 9. Setup event listeners (click, submit, etc)   │
        └───────────────────────────────────────────────────┘
                                    ↓
        ┌───────────────────────────────────────────────────┐
        │ Render to screen                                  │
        │ - Menu items visible (8 drinks)                  │
        │ - Products visible (6 coffee beans)              │
        │ - Gallery, reviews, footer all visible           │
        │ - Page fully loaded and interactive              │
        └───────────────────────────────────────────────────┘
```

---

## User Action - Add to Cart & Checkout

```
┌──────────────────────────────────┐
│ User clicks cart button           │
│ on "Espresso" menu item           │
└──────────────────────────────────┘
                ↓
┌────────────────────────────────────────────┐
│ addToCart(1, 'menu') called                │
│ - Find item: menuItems.find(i => i.id==1)│
│ - Add to: cart = [{id:1, ...}]            │
│ - Save state: localStorage.setItem(...)    │
│ - Update badge: updateCartBadge()          │
└────────────────────────────────────────────┘
                ↓
┌──────────────────────────────────┐
│ User clicks checkout button       │
└──────────────────────────────────┘
                ↓
┌────────────────────────────────────────────┐
│ Checkout modal opens                       │
│ - Show form (name, email, phone, etc)     │
│ - Show cart items                          │
│ - Show total                               │
└────────────────────────────────────────────┘
                ↓
┌──────────────────────────────────┐
│ User fills form & selects         │
│ payment method (card/UPI/COD)    │
└──────────────────────────────────┘
                ↓
┌──────────────────────────────────┐
│ User clicks "Place Order"         │
└──────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────────────┐
│ checkoutForm submit event handler:                   │
│ 1. Prevent default                                   │
│ 2. Get form data                                     │
│ 3. Validate all fields (client-side)                │
│ 4. Check cart not empty                             │
└──────────────────────────────────────────────────────┘
                ↓
        ┌─────────────────────────────────┐
        │ Validation passes? (YES)         │
        └─────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────────────┐
│ Prepare order data:                                  │
│ {                                                    │
│   orderType: 'pickup',                               │
│   customer: { name, email, phone, ... },            │
│   items: [{id:1, name:'Espresso', qty:1, ...}],    │
│   total: 7.50                                        │
│ }                                                    │
└──────────────────────────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────────────┐
│ Send to backend:                                     │
│ fetch('http://localhost:3000/api/orders', {         │
│   method: 'POST',                                    │
│   headers: {'Content-Type': 'application/json'},    │
│   body: JSON.stringify(orderData)                   │
│ })                                                   │
└──────────────────────────────────────────────────────┘
                ↓
    ┌──────────────────────────────────┐
    │ Backend /api/orders endpoint:    │
    │ 1. Validate request              │
    │ 2. INSERT INTO orders TABLE      │
    │ 3. Return response               │
    └──────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────────────┐
│ Frontend receives response:                          │
│ { id: 123, orderNumber: 'ORD-00123' }               │
│                                                      │
│ Show success message:                                │
│ "Order placed! Order #ORD-00123"                    │
└──────────────────────────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────────────┐
│ Clear state:                                         │
│ - Clear cart: cart = []                             │
│ - Save state: localStorage.clear()                   │
│ - Update display                                     │
└──────────────────────────────────────────────────────┘
```

---

## API Response Examples

### GET /api/menu
```json
[
  {
    "id": 1,
    "name": "Espresso",
    "description": "Rich and bold Italian classic",
    "price": 3.50,
    "image": "https://images.unsplash.com/...",
    "category": "espresso",
    "createdAt": "2025-11-28T10:00:00Z",
    "updatedAt": "2025-11-28T10:00:00Z"
  },
  {
    "id": 2,
    "name": "Cappuccino",
    "description": "Smooth espresso with steamed milk foam",
    "price": 4.50,
    "image": "https://images.unsplash.com/...",
    "category": "espresso",
    "createdAt": "2025-11-28T10:00:00Z",
    "updatedAt": "2025-11-28T10:00:00Z"
  }
  // ... 6 more items
]
```

### GET /api/products
```json
[
  {
    "id": 101,
    "name": "Ethiopian Blend",
    "description": "Fruity and floral notes",
    "price": 18.99,
    "rating": 5,
    "image": "https://images.unsplash.com/...",
    "createdAt": "2025-11-28T10:00:00Z",
    "updatedAt": "2025-11-28T10:00:00Z"
  }
  // ... 5 more products
]
```

### POST /api/orders
**Request:**
```json
{
  "orderNumber": "ORD-001",
  "customerName": "John Doe",
  "customerEmail": "john@example.com",
  "customerPhone": "555-0123",
  "orderType": "pickup",
  "items": "[{\"id\":1,\"name\":\"Espresso\",\"quantity\":2,\"price\":3.50}]",
  "total": 7.00
}
```

**Response:**
```json
{
  "id": 1,
  "orderNumber": "ORD-001"
}
```

### GET /api/admin/stats
```json
{
  "menuItemCount": 8,
  "productCount": 6,
  "orderCount": 1,
  "totalRevenue": 7.00
}
```

---

## Database Schema (SQLite)

### menu_items Table
```sql
CREATE TABLE IF NOT EXISTS menu_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    price REAL NOT NULL,
    image TEXT,
    category TEXT DEFAULT 'espresso',
    createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
    updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### products Table
```sql
CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    price REAL NOT NULL,
    rating INTEGER DEFAULT 5,
    image TEXT,
    createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
    updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### orders Table
```sql
CREATE TABLE IF NOT EXISTS orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    orderNumber TEXT UNIQUE NOT NULL,
    customerName TEXT NOT NULL,
    customerEmail TEXT NOT NULL,
    customerPhone TEXT,
    orderType TEXT DEFAULT 'pickup',
    items TEXT NOT NULL,
    total REAL NOT NULL,
    status TEXT DEFAULT 'pending',
    createdAt DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## Technology Stack

```
FRONTEND
├─ HTML5
├─ CSS3 (Grid, Flexbox, Mobile-responsive)
├─ Vanilla JavaScript (ES6+)
│  ├─ Fetch API for HTTP requests
│  ├─ localStorage for client persistence
│  └─ Async/await for API calls
├─ External: Google Fonts, Unsplash images
└─ Served via: Python http.server (port 5520)

BACKEND
├─ Node.js runtime
├─ Express.js framework
│  ├─ CORS middleware
│  ├─ body-parser for JSON
│  └─ Route handlers for REST endpoints
├─ SQLite3 database driver
├─ npm for dependency management
└─ Runs on: http://localhost:3000

DATABASE
└─ SQLite3
   ├─ File-based database (brewluxe.db)
   ├─ Three tables (menu_items, products, orders)
   ├─ Automatic timestamps (createdAt, updatedAt)
   └─ Primary keys and relationships
```

---

This completes the full-stack architecture! 🎉
